<h1>Review Options</h1>
<% @responses.each do |response| %>
		<h2>
			<%= link_to(response.name, response_path(response)) %>
		<h2>
<% end %>


	<!-- <div class = "row">
  	<div class = "form-group col-md-3">
    <%#= f.label :name %><br>
    <%#= f.select(:name, Resource.all.map(&:name) %>
  </div>
	</div> -->




